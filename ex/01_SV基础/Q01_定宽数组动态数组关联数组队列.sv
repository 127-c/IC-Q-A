//=============================================================
// Q01_练习：定宽数组、动态数组、关联数组、队列
// 难度: ⭐⭐ | 目标: 掌握四种数组类型的声明和使用
//=============================================================

module tb_q01_arrays;

  initial begin
    $display("========== Q01: 数组类型练习 ==========");

    //----------------------------------------------------------
    // TODO 1: 定宽数组 (编译时大小确定)
    //   - 压缩数组: 类型后面声明范围
    //   - 非压缩数组: 变量名后面声明范围
    //----------------------------------------------------------
    // 填写: 声明一个 8-bit宽 x 4深的压缩定宽数组 packed_data
    // 填写: 声明一个 8-bit宽 x 4深的非压缩定宽数组 unpacked_data

    // bit [7:0][3:0] packed_data;     // <-- 压缩定宽数组 (你的答案)
    // bit [7:0] unpacked_data [3:0];  // <-- 非压缩定宽数组 (你的答案)

    // 验证: 对数组赋值并打印
    // packed_data = ...;
    // $display("packed_data = %h", packed_data);


    //----------------------------------------------------------
    // TODO 2: 动态数组 (运行时用 new[] 分配空间)
    //----------------------------------------------------------
    // 填写: 声明一个 int 类型的动态数组 dyn_arr, 大小为10
    // 填写: 用 for 循环给每个元素赋值 i*2

    // int dyn_arr[];           // <-- 声明
    // dyn_arr = new[10];       // <-- 分配内存
    // for (int i=0; i<10; i++)
    //   dyn_arr[i] = i * 2;


    //----------------------------------------------------------
    // TODO 3: 关联数组 (类似hash, 用于稀疏存储)
    //----------------------------------------------------------
    // 填写: 声明一个 key=bit[63:0], value=bit[63:0] 的关联数组
    // 填写: 存入 addr=64'h1000 -> data=64'hDEAD_BEEF
    // 填写: 用 exists() 检查 key 是否存在

    // bit [63:0] assoc_arr [bit[63:0]];   // <-- 声明
    // assoc_arr[64'h1000] = 64'hDEAD_BEEF; // <-- 存入
    // if (assoc_arr.exists(64'h1000))
    //   $display("Found: %h", assoc_arr[64'h1000]);


    //----------------------------------------------------------
    // TODO 4: 队列 (结合链表+数组, 支持任意位置增删)
    //----------------------------------------------------------
    // 填写: 声明一个 int 类型的队列 q
    // 填写: 用 push_back 插入 10,20,30
    // 填写: 用 pop_front 取出并打印第一个元素
    // 填写: 用 insert(2, 99) 在索引2处插入99
    // 填写: 用 delete(1) 删除索引1的元素

    // int q[$];                // <-- 声明队列
    // q.push_back(10);
    // q.push_back(20);
    // q.push_back(30);
    // $display("pop_front = %0d", q.pop_front());  // 10
    // q.insert(2, 99);
    // q.delete(1);
    // $display("q = %p", q);

    $display("========== 练习结束 ==========");
  end

endmodule
